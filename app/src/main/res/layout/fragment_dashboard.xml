<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="20dp">

        <!-- App Name -->
        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/swiftride"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="@color/purple_accent"
            android:layout_marginTop="54dp"
            android:layout_marginBottom="16dp" />

        <!-- Section Title -->
        <TextView
            android:id="@+id/tvMyBookings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/my_bookings"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:layout_marginBottom="16dp" />

        <!-- Image Card -->
        <ImageView
            android:id="@+id/ivBookingsImage"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:src="@drawable/nice"
            android:scaleType="centerCrop"
            android:layout_marginBottom="20dp"
            android:background="@drawable/bg_card_round"
            android:clipToOutline="true"
            android:adjustViewBounds="true" />

        <!-- Empty State -->
        <TextView
            android:id="@+id/tvNoBookings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_bookings_yet"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:layout_marginBottom="12dp" />

        <!-- CTA -->
        <TextView
            android:id="@+id/tvBrowseVehicles"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/browse_vehicles_to_make_a_new_booking"
            android:textSize="14sp"
            android:textColor="@color/purple_accent"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="@string/browse_vehicles"
            android:foreground="?attr/selectableItemBackground" />

        <!-- Logout -->
        <Button
            android:id="@+id/btnLogout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/log_out"
            android:textColor="@android:color/white"
            android:backgroundTint="#7B2CBF"
            android:layout_gravity="center"
            android:paddingHorizontal="24dp"
            android:paddingVertical="12dp"
            android:textStyle="bold"/>
    </LinearLayout>
</ScrollView>
